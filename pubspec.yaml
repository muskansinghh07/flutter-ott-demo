name: ott_demo_app
description: A simple OTT platform demo app

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_bloc: ^8.1.3
  
  # Video Player (Native Flutter - No compatibility issues!)
  video_player: ^2.8.1
  
  # UI Components
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true